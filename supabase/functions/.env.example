# Comma-separated origins only; do not include URL paths or trailing slashes.
ALLOWED_ORIGINS=https://greinermachine.github.io,http://127.0.0.1:8765,http://localhost:8765

# Store the real value with `supabase secrets set`; never commit it.
RATE_LIMIT_PEPPER=replace-with-at-least-24-random-characters
